<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>📝 My ToDo List</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f0f2f5;
            margin: 0;
            padding: 0;
        }

        header {
            background: #3f51b5;
            color: white;
            padding: 30px;
            text-align: center;
            font-size: 24px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }

        main {
            max-width: 600px;
            margin: 40px auto;
            padding: 20px;
            background: white;
            border-radius: 8px;
            box-shadow: 0 5px 10px rgba(0,0,0,0.1);
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        ul li {
            position: relative;
            background: #f9f9f9;
            padding: 12px 15px;
            margin-bottom: 10px;
            border-left: 5px solid #3f51b5;
            border-radius: 4px;
            transition: all 0.3s ease;
        }

        ul li:hover {
            background: #eef2ff;
        }

        .delete-btn,
        .edit-btn {
            position: absolute;
            top: 12px;
            font-size: 16px;
            cursor: pointer;
            text-decoration: none;
        }

        .delete-btn {
            right: 10px;
            color: #f44336;
        }

        .edit-btn {
            right: 40px;
            color: #ffc107;
        }

        form {
            margin-top: 30px;
        }

        label {
            font-weight: bold;
            margin-bottom: 8px;
            display: block;
            color: #333;
        }

        input[type="text"] {
            width: calc(100% - 20px);
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            margin-bottom: 12px;
            font-size: 16px;
        }

        input[type="submit"] {
            background-color: #3f51b5;
            color: white;
            padding: 10px 20px;
            border: none;
            font-size: 16px;
            border-radius: 4px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #2c3ca5;
        }
    </style>
</head>

<body>
    <header>
        📝 Hello, what would you like to accomplish today?
    </header>

    <main>
        <ul>
            <% todolist.forEach(function(todo, index) { %>
            <li>
                <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
                <a href="/todo/<%= index %>" class="edit-btn">✎</a>
                <%- todo %>
            </li>
            <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <label for="newtodo">Add a new task:</label>
            <input type="text" name="newtodo" id="newtodo" placeholder="e.g., Learn Docker Compose" required autofocus />
            <input type="submit" value="Add Task" />
        </form>
    </main>
</body>
</html>
